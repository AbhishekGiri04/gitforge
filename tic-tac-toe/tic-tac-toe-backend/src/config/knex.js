const knex = require('knex');\nconst config = require('../config/database');\n\n// Initialize Knex with SQLite configuration\nconst db = knex(config);\n\n// Test database connection\ndb.raw('SELECT 1')\n  .then(() => {\n    console.log('✅ SQLite database connected successfully');\n  })\n  .catch((err) => {\n    console.error('❌ SQLite database connection failed:', err.message);\n  });\n\nmodule.exports = db;", "oldStr": "", "path": "/Users/abhishekgiri/Downloads/GITHUB_PROJECTS/gitforge/tic-tac-toe/backend/src/config/knex.js"}]